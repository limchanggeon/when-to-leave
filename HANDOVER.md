# 인수인계 — 언제나가 (whenigo)

> 세션을 새로 시작할 때 이 문서부터 읽으면 된다.
> 마지막 정리: **2026-09-11**. Android GPS 지도 표시·버튼식 출발 알람 구현과 `0.1.2-alpha` 서명 APK 빌드를 완료했다. 새 APK는 로컬에 있으며 공개 배포 전이다. `0.1.1` 은 시계 알람이 아무 일도 하지 않아 폐기했다(ClockAlarm.then 버그). 이메일 인증은 운영 반영 및 사용자 정상 동작 확인 완료.
> 왜 그렇게 됐는지는 [docs/PLATFORM.md](docs/PLATFORM.md)에 더 자세히 적혀 있다.

## 무엇을 만드는가

**도착 시각에서 출발 시각을 거꾸로 계산하는 이동 비서.**
"11시까지 부산" 이라고 하면 "19:06에 나가세요" 라고 답한다.

지도 앱은 "지금 출발하면 언제 도착"까지만 답한다. 그 반대 질문은
열차 시간표 때문에 단순 뺄셈으로 풀리지 않는다 — 이 앱의 알맹이가 거기 있다.

- 운영: **https://whenigo.p-e.kr**
- 저장소: `limchanggeon/when-to-leave` (2026-09-08 공개 전환 기록, 비상업용 라이선스)
- React 웹과 **Capacitor Android 앱**을 공유 코드로 제공한다. 공개 Android는 `0.1.0-alpha`, 최신 로컬 빌드는 `0.1.2-alpha`다. iOS 앱은 미구현이다.

## Android GPS·출발 알람 — 2026-09-11 작업

로컬 Android `0.1.2-alpha` (`versionCode=3`) 구현. 기존 공개 다운로드는 아직
`0.1.0-alpha`이며, 이번 APK는 별도 설치해야 한다. 웹 서버 배포만으로 앱은 갱신되지 않는다.

- `@capacitor/geolocation`으로 Android 위치 권한 요청과 GPS 조회를 연결했다.
  정확한 위치·대략적 위치를 모두 처리하며, 동시에 조회하면 요청을 하나로 합친다.
- 앱 첫 화면과 결과 지도에 파란 현재 위치 점과 오차 반경을 표시한다.
  지도 진입 시 한 번 조회하고 `현재 위치` 버튼으로 갱신한다. 백그라운드 추적은 하지 않는다.
- 사용자는 **자동 등록 대신 버튼을 눌러 등록**을 선택했다.
  결과의 `출발 알람 설정`을 누르면 계산된 시·분과 목적지를 Android 시계 앱에 넘긴다.
  로그인·구글 캘린더 연결은 필요 없다. 시계 앱에서 확인·저장하며 앱은 저장 완료를 단정하지 않는다.
- 지난 시각과 시계 앱의 다음 발생 시각과 날짜가 다른 여정은 차단한다.
  `ACTION_SET_ALARM`은 날짜를 받지 않으므로, 먼 날짜를 오늘 알람으로 잘못 등록하지 않기 위한 제한이다.
- 웹은 기존 캘린더 기능을 유지한다. 위치 권한 거부 안내도 앱과 브라우저를 구분한다.
- 테스트 155개, 타입 검사, 앱 웹 자산 빌드, 서명된 릴리스 APK 빌드·서명 검증 통과.
- Android 에뮬레이터에서 권한 창→사용 중 허용→GPS 주입 좌표(대전시청)→지도 파란 점·오차 5m 표시를 확인했다.
- 빌드 JDK는 Java 21을 사용한다. 현재 Android Studio 내장 Java 25는 Gradle 8.14.3과
  맞지 않아 `Unsupported class file major version 69`로 실패한다.
  로컬 JDK 경로: `/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`.
- 전달용 APK: `android/app/build/outputs/apk/release/whenigo-0.1.2-alpha.apk` (versionCode 3).
  **0.1.1 은 쓰지 말 것 — 시계 알람이 아무 일도 하지 않는다**(ClockAlarm.then 버그).
  Gradle 원본은 같은 폴더의 `app-release.apk`이며 두 파일은 동일하다.
  실제 휴대전화의 GPS 수신과 시계 앱 저장·울림 확인은 남아 있다.

## 지금 어디까지 왔나

| | 상태 |
|---|---|
| 역산 엔진 | 동작. 이산 구간(시각표)으로 데드라인을 앞으로 전파한다 |
| 국내 경로 | 시내는 카카오, 시외는 TAGO 시각표를 우리가 조합 |
| 시각표 | 열차·고속버스·시외버스·항공·지하철 |
| 배포 | AWS EC2 서울, HTTPS, `update.sh` 한 줄로 재배포 |
| 백업 | 매일 새벽 4시(KST) 로컬 DB 스냅샷 14개 보관 + S3 업로드(09-08 운영 기록) |
| 로그인 | 자체(이메일)·카카오·구글. scrypt + 시도 횟수 제한 |
| 관리자 | `/admin` — 계정 열람·삭제·인증 처리·세션 끊기. 기록이 남는다 |
| 이메일 인증 | SES 전송 구현. 09-10 프로덕션 액세스 확인 기록. 이메일 인증으로 로그인 허용. 09-11 운영 배포 완료 |
| 캘린더 | 구글 캘린더에 출발 알람 등록 |
| 디자인 | "발차 안내판" 시각 언어 적용 완료 |
| 접근성 | 이전 검사에서 axe 위반 0건(홈·로그인·결과). 이번에는 재검사하지 않음 |
| Android | 로컬 `0.1.2-alpha`: GPS 현재 위치·오차 반경, 버튼식 출발 알람. 서명 APK 완성, 공개 다운로드는 아직 `0.1.0-alpha` |
| 언어 | 한국어·일본어·영어. 교통 데이터는 국내만 지원 |
| 조회 한도 | 비회원 브라우저에서 1회 체험, 회원 무료 3회/일·후원자 50회/일·무제한 등급 |
| 디자인 재작업 | 2026-09-04 — 흰 카드 제거, 판 하나로 통합, 알약 폐기 |

## 최근 완료한 내용과 확인 결과

- **09-07 경로 품질:** 버스 노선·정류소 연동, 서울 별도 API 처리, 터미널 좌표 검증, 시외 후보 양 끝 접근·도착 구간 연결 보완.
- **09-08 계정·운영:** 조회 한도와 후원 등급, 중복 차감 방지, 이메일 중복확인과 계정 인계 보안 수정, 관리자 사용자 상세, S3 백업 기록.
- **09-08 웹:** 모바일 헤더, 검색 외부 호출 병렬화, 초기 로딩 최적화, 페이지별 메타데이터·검색엔진 확인 자료, 없는 정적 파일 404 처리, 개인정보처리방침과 광고 설정 연동.
- **09-08 앱·언어:** Capacitor Android 앱과 시계 알람, 카카오 로그인 복귀 흐름, 하단 탭·설정, 서명 설정, `0.1.0-alpha` 다운로드 링크, 영어 추가와 지구본 언어 메뉴.
- **09-10 메일:** 최신 커밋 `b21382c`에 SES 프로덕션 액세스 확인 기록. 후속 변경 `49f609d`에서 이메일 인증 가입으로 전환했고 09-11 운영에 배포했다(아래 참고).

최신 로컬 확인: **155개 테스트 통과**, `pnpm typecheck`, `pnpm app:build`,
Java 21을 사용한 `assembleRelease`, APK 서명 검증 통과. 에뮬레이터에서 위치 권한과
GPS 지도 표시를 확인했다. 실제 휴대전화의 GPS·시계 앱 저장·울림, S3 백업 복원은 미검증이다.
Android 변경은 아직 커밋·푸시·공개 릴리스하지 않았다.

기존 `app:release` 스크립트와 `skills-lock.json`은 이후 커밋 `927b797`에 포함됐다.
현재 작업 트리에는 그 이후 Android GPS·알람 변경과 이 문서 갱신이 남아 있다.

## 구조

```
브라우저 → Caddy(443) → Express(127.0.0.1:8787) → SQLite
                              └ dist/ 정적 파일도 같은 서버가 낸다
```

**경로를 어떻게 만드나** (`server/index.ts`의 `searchKorea`)

```
40km 미만 → 카카오 대중교통만
40km 이상 → 카카오 + 시외 조합을 둘 다 돌려 합친다
```

둘 다 돌리는 이유가 중요하다. 카카오는 대전→서울에도 광역버스를 엮어
"성공"을 돌려준다. 그것만 믿으면 3시간짜리 버스 연속이 답이 되고
68분 KTX는 보이지도 않는다.

**시외 조합** (`server/intercity.ts`)

```
출발지 --(카카오 시내)--> 역/터미널/공항 --(TAGO 시각표)--> 허브 --(카카오)--> 목적지
```

| 파일 | 하는 일 |
|---|---|
| `src/engine/` | 역산 엔진. React·DOM 의존 없음 — 앱에 그대로 간다 |
| `src/adapters/`, `src/parse/`, `src/i18n/`, `src/alarm/` | 순수 TS. 앱과 공유할 것들 |
| `src/ui/` | 웹·앱 공용 React 화면. 앱에서는 하단 탭과 설정 화면 사용 |
| `src/native/`, `android/` | Capacitor 셸과 Android 네이티브 플러그인 |
| `server/kakaoTransit.ts` | 시내 경로 + 도보 |
| `server/intercity.ts` | 시외 조합, 허브 탐색 |
| `server/tago.ts`, `server/tagoSchedules.ts` | 시각표 |
| `server/routeTypes.ts` | 제공자 중립 응답 타입 |

## 배포

```bash
ssh -i <키파일> ubuntu@<서버 IP>
sudo /srv/whenigo/deploy/update.sh
```

> 저장소가 공개라 서버 주소와 키 이름은 여기 적지 않는다. IP 를 알면 22번
> 포트를 바로 두드릴 수 있고, 실제로 열려 있다. 값은 AWS 콘솔의 EC2
> 인스턴스 목록에 있다.

자세한 것은 [deploy/README.md](deploy/README.md). 서버 비밀값은
`/etc/whenigo.env`(root 0600), 빌드용 `VITE_*`는 `/srv/whenigo/.env`.

## 시각 언어 — "발차 안내판"

역 대합실 전광판에서 가져왔다. 자세한 원칙은 `docs/PLATFORM.md` 아래쪽.

- **판은 하나이고 자르지 않는다.** 머리·답·검색이 전부 같은 판 위에 있고
  가로 괘선으로만 나뉜다. 판 위에 흰 카드를 띄우지 않는다 — 검색도 판에
  새긴 한 줄이고, 입력은 상자가 아니라 밑줄 그은 빈칸이다
- **판에는 늘 답이 걸린다.** 물어보기 전에는 불 꺼진 자릿수(`88:88`)가.
  답과 크기·굵기·자간이 같아서, 계산하면 그 자리에 그대로 불이 들어온다
- **화면에서 제일 큰 것은 언제나 시각이다.** 날짜는 딱지로 떼어 옆에
- **상자를 늘어놓지 않는다.** 머리말과 선으로 나눈다
- **호박색은 지금 중요한 것에만**(`--signal`) — 답·주 동작·켜져 있는 것.
  hover 로는 켜지 않는다
- **반경에 계단을 둔다.** 판 위는 각지게(2–4px), 밝은 종이 위만 6px.
  알약은 아바타와 카카오·구글 버튼에만
- 숫자는 전부 `.num`(Archivo, tabular-nums)
- 연출하는 순간은 둘뿐 — 계산 중 자릿수가 깜빡이고, 답이 걸릴 때 올라온다
- `word-break: keep-all` 없으면 한글이 "계산합 / 니다"로 잘린다

## 디자인 스킬

`.agents/skills/`에 15개 설치돼 있다(`.claude/skills/`로 심링크).
**세션을 새로 시작하면** `Skill` 도구가 인식한다 — `frontend-design`,
`emil-design-eng`, `improve-animations`, `make-interfaces-feel-better`,
`web-design-guidelines`, `ui-ux-pro-max` 등.

접근성 스킬(`supercent-io/skills-template@web-accessibility`)만 못 받았다.
**저장소가 404다.** 다른 소스를 찾거나 직접 챙겨야 한다.

## 가입 관문 — 이메일 인증으로 전환 (2026-09-11 운영 배포)

SES 설정과 테스트 발송은 완료됐다는 사용자 확인에 따라, 이메일 인증으로 가입이
완료되도록 연결했다. **2026-09-11 08:47 KST 운영 배포 완료.**

- `canSignIn = u.emailVerified || u.approved`: 이메일 소유 확인 또는 기존 수동 승인으로 로그인한다.
- 가입 → SES 인증 메일 → 24시간 유효한 일회용 링크 → 이메일 확인·웹 세션 발급.
- 인증 전 비밀번호 로그인은 `email-unverified`로 차단하고 재발송을 안내한다.
- 가입 메일 발송 실패는 503으로 알린다. 재발송은 계정 존재 여부를 숨기는 공통 응답을 유지한다.
- 소셜 제공자가 확인한 이메일도 인정한다. 확인된 이메일이 없는 소셜 계정은 수동 승인이 필요하다.
- 미인증 계정에 소셜 인증을 연결할 때 기존 비밀번호·토큰·세션을 제거해 사전 가입을 통한 탈취를 막는다.
- 수동 승인 회수는 이메일 인증 계정을 차단하지 않는다. 이용 정지는 별도 기능이 필요하다.
- 앱에서 이메일 링크를 열면 웹에서 인증된다. 앱으로 돌아가 다시 로그인한다.

검증: 136개 테스트, 타입 검사, 웹 빌드 통과. 격리된 DB와 콘솔 전송기로 실제 HTTP
가입→인증 전 차단→인증·세션 발급→로그인→사용한 링크 재사용 거부까지 확인했다.
운영 HTTPS 상태·로그인 번들·잘못된 인증 링크 처리를 확인했다. 이후 운영 임시 계정으로
인증 전 차단→유효한 링크 확인→세션 발급→로그아웃→비밀번호 재로그인도 확인했다.
메일은 발송하지 않고 토큰을 발급해 점검했으므로 실제 수신함을 거치는 확인은 남아 있다.

## 배포 기록 — 2026-09-11 08:47 KST

- 배포 코드: `49f609d` (이메일 인증으로 가입 완료). 이전 운영 코드: `c1caa43`.
- `main` 푸시 후 기존 `deploy/update.sh`로 의존성 설치·웹 빌드·서비스 재시작 완료.
- 배포 직전 백업: `/var/backups/whenigo/app-20260911-0847.db.gz`.
  같은 파일의 S3 업로드 성공을 서비스 로그에서 확인했다. 복원 시험은 하지 않았다.
- 서비스: `active/running`, 재시작 직후 `NRestarts=0`.
- 공개 HTTPS: `/api/health` 200, `missingEnv=[]`, `mail=null`; `/api/auth/me`는 비로그인 상태에서 `account=null`.
- `/login` 200 및 배포된 JS의 새 이메일 인증 안내 확인.
- 잘못된 인증 토큰: `/?verify=unknown`으로 302, 세션 쿠키 발급 없음.
- 이번 변경은 DB 스키마 변경이 없다. 실메일 발송이나 실제 사용자 계정 조작은 배포 검증 중 수행하지 않았다.
- 장애 시 이전 코드 `c1caa43`으로 되돌려 설치·빌드·서비스 재시작한다. 코드 롤백에는 DB 복원이 필요하지 않다.
  단, 이전 코드는 관리자 승인만 허용하므로 새로 이메일 인증한 사용자는 승인이 없으면 로그인할 수 없다.
- `package.json`의 기존 `app:release` 추가와 추적되지 않은 `skills-lock.json`은 이번 배포 커밋에 포함하지 않았다.

## 승인 대기 제보 후 확인 — 2026-09-11

사용자는 전날 저녁 이메일·비밀번호 가입 후 인증 링크에서 승인 대기 안내를 봤다고 했다.
자동 가입 코드는 09-11 08:47 KST에 배포됐으므로 그전 시도에는 이전 승인 정책이 적용됐다.

- 운영에 별도 임시 계정을 만들어 실제 HTTPS 인증 경로를 점검했다.
- 인증 전 비밀번호 로그인은 403 `email-unverified`.
- 유효한 링크는 `verify=ok`로 이동하고 세션을 발급했다.
- `/api/auth/me`에서 `emailVerified=true`, `approved=false`인 상태로 로그인됨을 확인했다.
- 로그아웃 후 이메일·비밀번호 로그인도 200. 검증용 계정과 관련 데이터는 제거했다.
- 실제 사용자 계정을 변경하거나 인증 메일을 발송하지 않았다.
- 예전 `verify=pending` URL의 한국어·영어·일본어 안내에서 관리자 승인 대기 문구를 제거했다.
- 과거 인증을 이미 마친 계정도 현재 `canSignIn` 규칙에 따라 비밀번호로 로그인할 수 있다.
  사용한 링크를 재사용해 세션을 발급하지는 않는다.

## 비밀번호 조건 표시 — 2026-09-11 확인

가입할 때 무엇을 채웠는지 입력하는 동안 보여준다.
규칙은 `src/auth/passwordRules.ts` **한 곳**에 있고 서버와 화면이 같이 본다 —
두 벌이면 "체크가 다 초록인데 거부당함" 이 나므로, 두 답이 갈리지 않는지
시험(`server/password.test.ts`)으로 묶어뒀다.

확인한 것: 웹(로컬·운영)과 **Android 에뮬레이터**에서 미충족 → 부분 →
전부 충족 → 경고(흔한 비밀번호) 네 단계를 눈으로 봤다.

에뮬레이터에서 잡은 것: `.emailauth__hint--bad` 가 **뒤에 오는**
`.emailauth__hint` 에 덮여 경고가 평범한 회색으로 보이고 있었다.
특정도가 같으면 나중 것이 이긴다. 수식 클래스를 기본 규칙 뒤로 옮겼다.
이 폼의 **모든** 경고 문구가 같이 먹히던 버그다.

## 시계 알람이 죽어 있던 버그 — 2026-09-11

에뮬레이터에서 목원대학교 → 대전역을 검색하고 `출발 알람 설정` 을 눌렀더니
아무 일도 일어나지 않았다. 로그에 이것이 찍혀 있었다:

    Uncaught (in promise) Error: "ClockAlarm.then()" is not implemented on android

**async 함수가 카파시터 플러그인 Proxy 를 그대로 return 하면 안 된다.**
프록시는 어떤 속성을 물어도 메서드로 받아주는데, async 함수는 반환값이
thenable 인지 보려고 `.then` 을 건드린다. 그 순간 프록시가 그것마저
플러그인 메서드로 여기고 터진다. 상자에 담아 돌려주면 검사가 상자를 보고 끝난다.

시험은 **초록이었다.** 모의가 평범한 객체라 `then` 이 없었기 때문이다.
모의를 진짜처럼 Proxy 로 바꿨더니 옛 코드에서 운영과 같은 오류로 실패한다.
붙잡아 두는 시험이 없으면 같은 모양으로 또 깨진다.

고친 뒤 확인: 시계 앱이 열리고 `대전역 출발 / 9:36 AM / Today` 로 넘어갔다.
플러그인 호출도 로그에 남는다 — `{"hour":9,"minute":36,"label":"대전역 출발"}`
→ `{"opened":true}`.

## 다음에 할 일

1. **개시 직전에 할 일 — 도메인 옮기기.** 아래 "개시 전 체크리스트" 참고.
   애드센스가 여기 걸려 있다
2. **버스 실시간** — 정류소·노선 정보는 2026-09-07 에 신청해 열렸고
   `server/busRoutes.ts` 가 쓰고 있다. 남은 것은 **도착 예정**이다 —
   시내버스가 "평균 16분 간격 · 추정" 에서 **"다음 213번 7분 후"** 로 바뀐다.
   화면에서 체감이 제일 큰 변화다
3. **AI 층** — 설계상 양 끝에만 둔다.
   - 말 → 질의 ("내일 아침 일찍 부산")
   - 후보 → 추천·설명 (요금·환승·시간대를 견줘서)
   - **탐색과 시각표 계산은 AI에게 맡기지 않는다.** 결정적이어야 한다
4. **시외 조합 품질** — 허브 고르기는 좌표표로 많이 나아졌다
   (`server/terminalIndex.ts`). 남은 것은 좌표를 못 믿어 표에서 뺀 133곳이다
5. **Android 실기기 검증·배포** — `whenigo-0.1.2-alpha.apk`를 기존 앱 위에 설치해 GPS 권한 허용·거부, 지도 위치 갱신, 경로 계산 후 알람 버튼과 실제 울림을 확인한다. 확인 후 변경 커밋·푸시, 새 GitHub 릴리스 APK 업로드, 웹 푸터의 버전 고정 다운로드 주소 갱신을 진행한다. iOS와 AlarmKit은 미구현이다.
6. **계정 보완** — 카카오 웹 로그인 OAuth `state` 검증과 비밀번호 재설정은 README에 남아 있는 미구현 항목이다.

## 개시 전 체크리스트 — 도메인부터 옮긴다

**`whenigo.p-e.kr` 로는 애드센스를 못 단다.** 확인한 사실이다(2026-09-08):

- `p-e.kr` 은 Public Suffix List 에 **없다**(10,323개 중 0건). 그래서 구글은
  `p-e.kr` 하나를 사이트로 보고 `whenigo` 를 그 안의 폴더로 친다
- 애드센스 콘솔이 `whenigo.p-e.kr` 등록을 **거부한다** —
  "URL은 올바른 최상위 도메인이어야 합니다"
- 등록되는 것은 `p-e.kr` 뿐인데 그건 남의 도메인이다. 접속도 안 되고
  (HTTP 000, 다른 IP), 거기 `ads.txt` 를 올릴 수 없으니 수익 배분이
  성립하지 않는다

`github.io` 나 `pages.dev` 는 PSL 에 있어서 하위 도메인이 독립 사이트로
인정된다. `p-e.kr` 은 아니다. **콘솔에서 우회할 방법이 없다.**

### 옮길 때 할 일

`.kr` 이나 `.co.kr` 하나면 된다(둘 다 PSL 에 있다, 연 1~2만원).
`whenigo.kr` 과 `whenigo.co.kr` 은 2026-09-08 기준 DNS 가 비어 있었다.

1. DNS A 레코드 → 지금 인스턴스의 탄력적 IP(AWS 콘솔에서 확인)
2. `deploy/Caddyfile` 의 도메인 교체 → 인증서는 Caddy 가 알아서 받는다
3. `/etc/whenigo.env` 의 `PUBLIC_ORIGIN`, `GOOGLE_REDIRECT_URI` 갱신
4. 구글 클라우드 콘솔·카카오 개발자센터의 리다이렉트 URI 갱신
5. **SES 발신 도메인 재인증** — 지금 `whenigo.p-e.kr` 로 돼 있다.
   DNS 전파를 기다려야 할 수 있다
6. `public/robots.txt`·`public/sitemap.xml`·개인정보처리방침의 주소 갱신
7. 옛 주소에서 새 주소로 301
8. **프레임 차단을 구글에만 연다.** 애드센스 확인 도구가 사이트를 프레임에
   띄우는데 지금은 `frame-ancestors 'none'` 이 막는다. 확인했다:
   `Framing 'https://whenigo.p-e.kr/' violates ... "frame-ancestors 'none'"`.
   `X-Frame-Options` 는 허용 목록을 못 쓰므로 지우고 CSP 로만 막는 편이 낫다

`ads.txt`·`robots.txt`·`sitemap.xml`·개인정보처리방침은 이미 올려뒀다.
새 도메인에서 주소만 바꾸면 그대로 쓴다.

## 밟으면 아픈 곳

- **`VITE_` 접두사를 서버 비밀값에 붙이지 말 것.** Vite가 클라이언트
  번들에 그대로 박아 넣는다. `client_secret`이 새는 순간 서버를 둔 의미가 없다
- **`VITE_` 키는 애초에 숨길 수 없다.** 빌드가 클라이언트 번들에 그대로
  박아 넣으므로 사이트를 연 사람은 누구나 볼 수 있다. 초기 커밋 `710ebb5`의
  `.env.example` 에 카카오 JS 키와 구글 지도 키가 값째로 들어갔다가 지워졌고
  히스토리에는 남아 있는데, **그게 문제가 아니다** — 같은 값이 지금도 번들에
  있다. 히스토리를 지워봐야 얻는 게 없다.
  지키는 방법은 **콘솔에서 도메인을 묶는 것**이다. 둘 다 걸려 있는 것을
  확인했다(2026-09-08): 남의 Referer 로 부르면 카카오는 401, 구글은 403 이다.
  저장소는 2026-09-08 에 공개로 돌렸다
- **`GOOGLE_CLIENT_ID` 는 운영에서 두 파일에 다 있어야 한다.** 서버는
  `/etc/whenigo.env` 만 읽는데, 코드의 `VITE_GOOGLE_CLIENT_ID` 대체는 두 값이
  한 파일에 있는 로컬에서만 걸린다. 빠뜨리면 로컬에서 멀쩡한 구글 로그인이
  운영에서만 죽는다 (2026-09-04에 실제로 그랬다). `/api/health` 가 알려준다
- **가입은 이메일 인증 또는 수동 승인으로 열린다.** 수동 승인을 거둬도 이메일이
  확인된 계정은 다시 로그인할 수 있다. 승인 회수를 이용 정지 기능으로 사용하지 말 것.
- **SES 프로덕션 액세스는 2026-09-10 에 열렸다.** 인증되지 않은 주소로도
  메일이 나간다. 확인한 방법: 자기 주소의 **플러스 주소**로 보내본다
  (`me+test@gmail.com`) — 편지함은 같지만 SES 는 별개 신원으로 보므로,
  샌드박스면 반드시 거부된다. 남의 편지함을 건드리지 않는 결정적 시험이다
- **IAM 사용자에 `ses:GetAccount` 권한이 없다.** 그래서 API 로는 샌드박스
  여부를 못 읽는다(403). 발송 전용으로 좁게 준 결과라 그대로 둔다 —
  상태는 위의 플러스 주소 시험으로 확인한다
- **메일이 안 나갔으면 가입 화면이 그렇게 말한다**(503 `mail-unavailable`).
  "보냈습니다" 라고 해놓고 안 보내면 기다리는 사람은 받은편지함만 새로고침
  하다 만다. 이걸 알려도 어떤 주소가 가입돼 있는지는 새지 않는다 — 두 갈래가
  모두 메일을 보내므로 실패는 주소와 무관하다. **다시 보내기는 다르다**:
  거기는 계정이 있을 때만 보내므로, 실패를 알리면 그게 곧 "가입돼 있다" 는
  답이 된다. 그래서 그쪽은 지금처럼 늘 `{sent:true}` 로 둔다
- **`/api/health` 가 마지막 발송 실패를 보여준다.** 설정이 다 맞아도(전송기
  ses, 키 있음) 샌드박스나 한도로 안 나갈 수 있는데, 그건 환경변수 점검으로는
  못 잡는다 — 보내 봐야 안다
- **배포는 락파일의 공급망 정책을 통과해야 한다.** 발행된 지 얼마 안 된
  패키지가 물려 있으면 `pnpm install --frozen-lockfile` 이 거절하고 배포가
  거기서 멈춘다(운영은 이전 버전 그대로라 안전하다). 지금은 그래서
  `pnpm-workspace.yaml` 의 `overrides` 로 `tinyspy` 를 4.0.4 에 묶어뒀다 —
  시간이 지나 최신 버전이 정책을 통과하면 그 고정은 지워도 된다
- **서버는 한국 시각으로 생각해야 한다.** TAGO 시각표는 "0536" 같은 벽시계
  숫자로 오는데, 그걸 Date 로 바꾸는 코드가 프로세스의 지역 시간대를 따른다.
  운영 서버는 Etc/UTC 라 노트북(KST)에서는 맞고 운영에서만 9시간 밀렸다
  (2026-09-05에 실제로 그랬다 — 첫차가 오후 2시로 잡혀 "경로를 찾지
  못했습니다"가 떴다). `server/timezone.ts` 가 첫 import 로 못 박는다.
  **시각을 다루는 시험은 지역 시간대가 아니라 절대 시각(ISO)으로 확인할 것** —
  안 그러면 버그가 있는 채로도 통과한다
- **첫 관리자는 화면에서 못 만든다.** 그런 입구가 있으면 그게 곧 뒷문이다.
  서버에서 `pnpm admin:grant <이메일>` 로 세운다(`admin:list`, `admin:revoke` 도 있다).
  자기 계정 삭제와 자기 권한 회수는 화면에서 막혀 있다 — 관리자가 0명이 되면
  화면으로는 아무도 못 들어간다
- **백업은 S3 로도 올라간다**(2026-09-08 부터, `s3://whenigo-backup/whenigo/`).
  버킷은 서울 리전이고 퍼블릭 액세스는 전부 막혀 있다. 권한은 IAM 사용자에
  붙인 `s3:PutObject` 하나뿐이라 서버는 올리기만 하고 읽지 못한다 —
  복구는 사람이 콘솔에서 내려받아서 한다. **리전을 옮기면 서명이 안 맞아
  업로드가 실패한다**(`deploy/README.md` 참고)
- **시도 횟수 제한은 프로세스 메모리에 있다.** 서버가 하나라 충분하지만,
  늘리는 순간 서버마다 따로 세어 제한이 사실상 N배로 헐거워진다.
  서버를 늘릴 거면 `server/rateLimit.ts` 의 저장소부터 옮겨야 한다
- **TAGO 오퍼레이션 대소문자가 서비스마다 다르다.** 시각표 계열은 대문자,
  실시간 계열은 소문자. 틀리면 키 문제처럼 보이는 `[12]`가 온다
- **TAGO 키는 서비스마다 따로 신청해야 한다.** 미신청이면 `[30]`
- **토요일 시각표 코드(02)가 비는 노선이 있다.** 데이터 누락이 아니라
  **그 노선이 토요일에 휴일 시각표로 운행**하기 때문이다(공항철도처럼 평일·휴일
  두 벌로만 나누는 곳). 그래서 토요일은 02 → 03 순으로 물어본다. 서울 1~9호선처럼
  세 벌인 곳은 02 에서 바로 걸린다
- **기준일(오늘)이 통째로 비면 시각표를 아예 주지 않는다.** 내일 것만 넘기면
  엔진이 출발을 내일 새벽으로 밀어버린다("내일 05:06 에 나가세요").
  열차·버스(`server/tago.ts`)도 같은 모양의 `if (!rows) continue` 를 갖고 있는데,
  거긴 호출 **실패**(null)와 **진짜 빈 결과**([])를 구분해야 한다 —
  전자는 모르는 것이고 후자는 그날 운행이 없다는 정보다
- **문의는 저장이 먼저고 메일은 그다음이다**(`server/contact.ts`). 저장되면
  접수된 것이고 메일은 알림 수단이다. 발송 실패는 관리자 문의함에 표시한다.
  SES 프로덕션 액세스가 열려도 전송 장애에 대비한 이 구조는 유지한다.
- **터미널 좌표는 시각표로 검증한다**(`pnpm terminals:build` → `terminals:verify`).
  TAGO 가 좌표를 안 줘서 카카오로 지오코딩하는데, 짧은 이름은 엉뚱한 곳을
  짚는다("상봉"이 진주로, "김포"가 고양으로). 사람이 700곳을 검수할 수는
  없으므로 시각표가 대신 따진다 — 닻이 되는 큰 터미널까지의 소요 시간과
  좌표상 직선 거리를 견줘, 버스가 낼 수 없는 속도(25~110km/h 밖)면 버린다.
  **닻 둘이 동의해야** 통과다. 하나만 보면 우연에 속는다 — "연무대"(논산)가
  동수원으로 찍혔는데 부산까지 거리가 비슷해 한 닻으로는 그냥 통과했다
- **좌표표는 더하기만 한다.** 표에 없는 터미널은 예전처럼 이름으로 맞추므로
  표가 비어도 오늘 되는 것은 그대로 된다. 판단이 안 서는 133곳을 굳이 넣지
  않는 이유다 — 조용히 틀린 좌표가 못 찾는 것보다 나쁘다
- **TAGO 시내버스 데이터에 서울이 없다.** 버스노선정보·정류소정보는 지자체가
  넘겨준 것만 담는데 서울시는 자체 API 를 쓴다. 그래서 서울은
  `ws.bus.go.kr` 로 따로 묻는다(`server/busRoutes.ts` 의 `BusRegion`).
  인증키는 같은 공공데이터포털 키다
- **서울 API 는 https 가 안 된다.** TLS 핸드셰이크가 실패해서 http 로 부른다.
  서버끼리의 호출이고 노선 정류장 목록에는 비밀이 없어 감수한 것이다
- 서울 좌표를 TAGO 에 물으면 **인천(23)** 정류소가 오는 일이 있다(관악IC 같은
  광역버스 정류소). 그래서 서울 네모 안이면 서울 API 를 먼저 본다. 잘못
  골라도 정류장 이름과 좌표 200m 를 함께 보는 관문이 엉뚱한 노선을 걸러낸다 —
  인천 자료에 같은 번호가 있어도 안 걸리는 것을 시험했다(2026-09-07)
- **시외 후보에는 접근·도착 구간을 전부 붙인다.** 예전에는 호출을 아끼려고
  상위 두 개에만 붙였는데, 세 번째부터는 "유성복합터미널에서 출발" 로 시작하는
  반쪽짜리 경로가 되어 거기까지 가는 길도 없이 도보 0분·환승 없음이라고
  적혔다(2026-09-07). 후보는 많아야 수단 수(넷)라 나란히 돌리면 값이 얼마 안
  든다. **양 끝 중 하나라도 못 이으면 그 후보는 내놓지 않는다** —
  갈 방법을 못 찾았으면 그건 경로가 아니다
- **연장은 "마지막으로 타는 것" 이 버스일 때만 한다.** 버스 뒤에 지하철이
  오면 그 버스는 목적지가 아니라 환승역으로 가는 길이다. 늘리면 갈아탈 곳을
  지나쳐 버린다 — 강남역 → 건국대에서 논현역 대신 광림교회까지 타라는 답이
  나왔다(둘 다 건국대와 멀다)
- **노선 목록은 갈 때와 올 때가 한 줄로 이어져 있다.** 603 은 100곳 중 31곳이
  이름이 두 번 나온다(목원대학교가 순번 1과 100). **정류장은 반드시 좌표로
  짚을 것** — 이름으로 첫 번째를 잡으면 반대 방향을 짚어 왔던 길을
  되돌아가라는 답이 된다
- **TAGO 는 터미널만 알고 경유 정류소는 모른다.** 대전 → 인천공항 리무진은
  실제로 정부청사(둔산)에 서지만, 그 정류소는 시외 339곳·고속 404곳 목록
  어디에도 없다. 그래서 둔산동 사람도 대전복합터미널까지 가서 타라고 안내된다.
  시간표 없이 "대전복합 출발 +20분" 으로 추정해 채우지 말 것 — 몇 시에 나가야
  하는지가 전부인 앱에서 출발 시각을 지어내면 그게 제일 큰 거짓말이 된다
- **허브 후보는 TAGO 가 아는 이름만 남긴다**(`hubsNear`). 카카오는 작은
  정류소까지 알려주는데, 조회할 수 없는 이름이 세 자리를 차지하면 한 번도
  물어보지 못한 채 후보가 끝난다. 실제로 둔산동에서 시외 허브 세 자리를
  대전청사 정류소 셋(0.8·1.2·1.3km)이 가져가, 공항버스가 뜨는
  대전복합터미널(4.7km)이 밀려나 환승 3회짜리 시내 경로가 답이 됐다
  (2026-09-05). 목록을 못 받아왔을 때는 **전부 통과시킨다** — 거기서 걸러버리면
  TAGO 가 잠깐 죽었을 때 시외 경로가 통째로 사라진다
- **인천공항은 T2 로 들어가 T1 으로 건너간다.** 시외버스 목록에 인천공항1터미널이
  없고 2터미널만 있다(고속에는 둘 다 있지만 대전발 편성이 0이다). T1 행 경로에
  공항 셔틀 구간이 붙는 건 그래서다
- **p-e.kr은 Public Suffix List에 없다.** Let's Encrypt 한도를 남과 나눠 쓴다
  (Caddy가 실패하면 ZeroSSL로 넘어가므로 대개는 발급된다)
- **사용법(`Tour.tsx`)은 선택자로 화면을 짚는다.** `#field-to`, `.panel__submit`
  같은 자리를 가리키므로, 그 선택자를 바꾸면 짚을 곳을 잃는다. 요소를 옮기거나
  클래스를 갈 때는 `STEPS` 를 같이 볼 것
- **없는 데이터는 없다고 말한다.** 어댑터 실패는 예외가 아니라 값(`{ok:false}`)이고
  화면은 `<DataGap>`으로 그 사실을 보여준다. 추정치로 메우지 않는다

## 개발

```bash
pnpm dev:all      # vite + api 동시에
pnpm build
pnpm exec vitest run
pnpm exec tsc -b --force && pnpm exec tsc --noEmit -p tsconfig.server.json
```
