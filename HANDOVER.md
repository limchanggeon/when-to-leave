# 인수인계 — 언제나가 (whenigo)

> 세션을 새로 시작할 때 이 문서부터 읽으면 된다.
> 왜 그렇게 됐는지는 [docs/PLATFORM.md](docs/PLATFORM.md)에 더 자세히 적혀 있다.

## 무엇을 만드는가

**도착 시각에서 출발 시각을 거꾸로 계산하는 이동 비서.**
"11시까지 부산" 이라고 하면 "19:06에 나가세요" 라고 답한다.

지도 앱은 "지금 출발하면 언제 도착"까지만 답한다. 그 반대 질문은
열차 시간표 때문에 단순 뺄셈으로 풀리지 않는다 — 이 앱의 알맹이가 거기 있다.

- 운영: **https://whenigo.p-e.kr**
- 저장소: `limchanggeon/when-to-leave` (비공개)
- 웹이 먼저지만 **안드로이드·iOS 앱도 만들 예정**이라 그걸 염두에 두고 층을 나눴다.

## 지금 어디까지 왔나

| | 상태 |
|---|---|
| 역산 엔진 | 동작. 이산 구간(시각표)으로 데드라인을 앞으로 전파한다 |
| 국내 경로 | 시내는 카카오, 시외는 TAGO 시각표를 우리가 조합 |
| 시각표 | 열차·고속버스·시외버스·항공·지하철 |
| 배포 | AWS EC2 서울, HTTPS, `update.sh` 한 줄로 재배포 |
| 백업 | 매일 새벽 4시(KST) DB 스냅샷 14개 보관. **원격 사본은 아직 없다** |
| 로그인 | 자체(이메일)·카카오·구글. scrypt + 시도 횟수 제한 |
| 관리자 | `/admin` — 계정 열람·삭제·인증 처리·세션 끊기. 기록이 남는다 |
| 이메일 인증 | 코드는 완성. **전송기가 없어 아직 배포 안 함** — SES 설정이 남았다 |
| 캘린더 | 구글 캘린더에 출발 알람 등록 |
| 디자인 | "발차 안내판" 시각 언어 적용 완료 |
| 접근성 | axe 위반 0건(홈·로그인·결과) |
| 디자인 재작업 | 2026-09-04 — 흰 카드 제거, 판 하나로 통합, 알약 폐기 |

## 구조

```
브라우저 → Caddy(443) → Express(127.0.0.1:8787) → SQLite
                              └ dist/ 정적 파일도 같은 서버가 낸다
```

**경로를 어떻게 만드나** (`server/index.ts`의 `searchKorea`)

```
40km 미만 → 카카오 대중교통만
40km 이상 → 카카오 + 시외 조합을 둘 다 돌려 합친다
```

둘 다 돌리는 이유가 중요하다. 카카오는 대전→서울에도 광역버스를 엮어
"성공"을 돌려준다. 그것만 믿으면 3시간짜리 버스 연속이 답이 되고
68분 KTX는 보이지도 않는다.

**시외 조합** (`server/intercity.ts`)

```
출발지 --(카카오 시내)--> 역/터미널/공항 --(TAGO 시각표)--> 허브 --(카카오)--> 목적지
```

| 파일 | 하는 일 |
|---|---|
| `src/engine/` | 역산 엔진. React·DOM 의존 없음 — 앱에 그대로 간다 |
| `src/adapters/`, `src/parse/`, `src/i18n/`, `src/alarm/` | 순수 TS. 앱과 공유할 것들 |
| `src/ui/` | 화면. 앱으로 갈 때 다시 짜야 하는 부분 |
| `server/kakaoTransit.ts` | 시내 경로 + 도보 |
| `server/intercity.ts` | 시외 조합, 허브 탐색 |
| `server/tago.ts`, `server/tagoSchedules.ts` | 시각표 |
| `server/routeTypes.ts` | 제공자 중립 응답 타입 |

## 배포

```bash
ssh -i ~/Downloads/whenigo.pem ubuntu@52.78.206.253
sudo /srv/whenigo/deploy/update.sh
```

자세한 것은 [deploy/README.md](deploy/README.md). 서버 비밀값은
`/etc/whenigo.env`(root 0600), 빌드용 `VITE_*`는 `/srv/whenigo/.env`.

## 시각 언어 — "발차 안내판"

역 대합실 전광판에서 가져왔다. 자세한 원칙은 `docs/PLATFORM.md` 아래쪽.

- **판은 하나이고 자르지 않는다.** 머리·답·검색이 전부 같은 판 위에 있고
  가로 괘선으로만 나뉜다. 판 위에 흰 카드를 띄우지 않는다 — 검색도 판에
  새긴 한 줄이고, 입력은 상자가 아니라 밑줄 그은 빈칸이다
- **판에는 늘 답이 걸린다.** 물어보기 전에는 불 꺼진 자릿수(`88:88`)가.
  답과 크기·굵기·자간이 같아서, 계산하면 그 자리에 그대로 불이 들어온다
- **화면에서 제일 큰 것은 언제나 시각이다.** 날짜는 딱지로 떼어 옆에
- **상자를 늘어놓지 않는다.** 머리말과 선으로 나눈다
- **호박색은 지금 중요한 것에만**(`--signal`) — 답·주 동작·켜져 있는 것.
  hover 로는 켜지 않는다
- **반경에 계단을 둔다.** 판 위는 각지게(2–4px), 밝은 종이 위만 6px.
  알약은 아바타와 카카오·구글 버튼에만
- 숫자는 전부 `.num`(Archivo, tabular-nums)
- 연출하는 순간은 둘뿐 — 계산 중 자릿수가 깜빡이고, 답이 걸릴 때 올라온다
- `word-break: keep-all` 없으면 한글이 "계산합 / 니다"로 잘린다

## 디자인 스킬

`.agents/skills/`에 15개 설치돼 있다(`.claude/skills/`로 심링크).
**세션을 새로 시작하면** `Skill` 도구가 인식한다 — `frontend-design`,
`emil-design-eng`, `improve-animations`, `make-interfaces-feel-better`,
`web-design-guidelines`, `ui-ux-pro-max` 등.

접근성 스킬(`supercent-io/skills-template@web-accessibility`)만 못 받았다.
**저장소가 404다.** 다른 소스를 찾거나 직접 챙겨야 한다.

## 다음에 할 일

1. **버스 실시간** — `버스도착정보` API는 동작하는데 정류소 `nodeId`가 없다.
   data.go.kr에서 **버스정류소정보**와 **버스노선정보**를 추가 신청하면
   시내버스가 "평균 16분 간격 · 추정" 에서 **"다음 213번 7분 후"** 로 바뀐다.
   화면에서 체감이 제일 큰 변화다.
2. **AI 층** — 설계상 양 끝에만 둔다.
   - 말 → 질의 ("내일 아침 일찍 부산")
   - 후보 → 추천·설명 (요금·환승·시간대를 견줘서)
   - **탐색과 시각표 계산은 AI에게 맡기지 않는다.** 결정적이어야 한다
3. **시외 조합 품질** — 허브를 늘 최단거리로만 고른다. 고속버스 후보가
   잘 안 잡히는 구간이 있다(TAGO 데이터가 비는 경우와 구분 필요)
4. **앱** — 공유 로직 약 1,800줄은 그대로 간다. 화면 2,600줄과
   플랫폼 다리(로그인·위치·지도) 900줄은 다시 짠다. Expo + dev build 권장

## 밟으면 아픈 곳

- **`VITE_` 접두사를 서버 비밀값에 붙이지 말 것.** Vite가 클라이언트
  번들에 그대로 박아 넣는다. `client_secret`이 새는 순간 서버를 둔 의미가 없다
- **저장소를 공개로 돌리지 말 것.** 초기 커밋 `710ebb5`의 `.env.example`에
  브라우저 키 두 개가 값째로 들어갔다가 지워졌다 — 히스토리에는 남아 있다
- **`GOOGLE_CLIENT_ID` 는 운영에서 두 파일에 다 있어야 한다.** 서버는
  `/etc/whenigo.env` 만 읽는데, 코드의 `VITE_GOOGLE_CLIENT_ID` 대체는 두 값이
  한 파일에 있는 로컬에서만 걸린다. 빠뜨리면 로컬에서 멀쩡한 구글 로그인이
  운영에서만 죽는다 (2026-09-04에 실제로 그랬다). `/api/health` 가 알려준다
- **이메일 인증은 전송기가 붙기 전에는 배포하면 안 된다.** `MAIL_TRANSPORT`
  가 `console` 이면 메일이 로그에만 찍혀서, 가입한 사람은 "메일을 보냈습니다"
  만 보고 영영 링크를 못 받는다. `/api/health` 의 `missingEnv` 가 이걸 잡는다
- **배포는 락파일의 공급망 정책을 통과해야 한다.** 발행된 지 얼마 안 된
  패키지가 물려 있으면 `pnpm install --frozen-lockfile` 이 거절하고 배포가
  거기서 멈춘다(운영은 이전 버전 그대로라 안전하다). 지금은 그래서
  `package.json` 의 `pnpm.overrides` 로 `tinyspy` 를 4.0.4 에 묶어뒀다 —
  시간이 지나 최신 버전이 정책을 통과하면 그 고정은 지워도 된다
- **서버는 한국 시각으로 생각해야 한다.** TAGO 시각표는 "0536" 같은 벽시계
  숫자로 오는데, 그걸 Date 로 바꾸는 코드가 프로세스의 지역 시간대를 따른다.
  운영 서버는 Etc/UTC 라 노트북(KST)에서는 맞고 운영에서만 9시간 밀렸다
  (2026-09-05에 실제로 그랬다 — 첫차가 오후 2시로 잡혀 "경로를 찾지
  못했습니다"가 떴다). `server/timezone.ts` 가 첫 import 로 못 박는다.
  **시각을 다루는 시험은 지역 시간대가 아니라 절대 시각(ISO)으로 확인할 것** —
  안 그러면 버그가 있는 채로도 통과한다
- **첫 관리자는 화면에서 못 만든다.** 그런 입구가 있으면 그게 곧 뒷문이다.
  서버에서 `pnpm admin:grant <이메일>` 로 세운다(`admin:list`, `admin:revoke` 도 있다).
  자기 계정 삭제와 자기 권한 회수는 화면에서 막혀 있다 — 관리자가 0명이 되면
  화면으로는 아무도 못 들어간다
- **백업은 S3 로도 올라간다**(2026-09-08 부터, `s3://whenigo-backup/whenigo/`).
  버킷은 서울 리전이고 퍼블릭 액세스는 전부 막혀 있다. 권한은 IAM 사용자에
  붙인 `s3:PutObject` 하나뿐이라 서버는 올리기만 하고 읽지 못한다 —
  복구는 사람이 콘솔에서 내려받아서 한다. **리전을 옮기면 서명이 안 맞아
  업로드가 실패한다**(`deploy/README.md` 참고)
- **시도 횟수 제한은 프로세스 메모리에 있다.** 서버가 하나라 충분하지만,
  늘리는 순간 서버마다 따로 세어 제한이 사실상 N배로 헐거워진다.
  서버를 늘릴 거면 `server/rateLimit.ts` 의 저장소부터 옮겨야 한다
- **TAGO 오퍼레이션 대소문자가 서비스마다 다르다.** 시각표 계열은 대문자,
  실시간 계열은 소문자. 틀리면 키 문제처럼 보이는 `[12]`가 온다
- **TAGO 키는 서비스마다 따로 신청해야 한다.** 미신청이면 `[30]`
- **토요일 시각표 코드(02)가 비는 노선이 있다.** 데이터 누락이 아니라
  **그 노선이 토요일에 휴일 시각표로 운행**하기 때문이다(공항철도처럼 평일·휴일
  두 벌로만 나누는 곳). 그래서 토요일은 02 → 03 순으로 물어본다. 서울 1~9호선처럼
  세 벌인 곳은 02 에서 바로 걸린다
- **기준일(오늘)이 통째로 비면 시각표를 아예 주지 않는다.** 내일 것만 넘기면
  엔진이 출발을 내일 새벽으로 밀어버린다("내일 05:06 에 나가세요").
  열차·버스(`server/tago.ts`)도 같은 모양의 `if (!rows) continue` 를 갖고 있는데,
  거긴 호출 **실패**(null)와 **진짜 빈 결과**([])를 구분해야 한다 —
  전자는 모르는 것이고 후자는 그날 운행이 없다는 정보다
- **문의는 저장이 먼저고 메일은 그다음이다**(`server/contact.ts`). SES 가
  샌드박스라 인증되지 않은 주소로는 못 보내는데, 메일만 시도했다면 보낸
  사람은 "보냈습니다" 를 보고 우리는 아무것도 못 받는다. 저장이 됐으면
  접수된 것이고, 메일은 그걸 알리는 수단일 뿐이다. 못 보낸 건 관리자
  문의함에 "메일 미발송" 으로 뜬다
- **SES 샌드박스에서는 받는 사람도 인증돼야 한다.** 프로덕션 액세스가
  풀리기 전에도, 받을 주소(`CONTACT_TO`)를 SES 콘솔에서 아이덴티티로
  인증해두면 문의 메일은 나간다. 인증 메일(임의의 가입자 주소)은 그 방법이
  안 통하므로 여전히 프로덕션 액세스를 기다려야 한다
- **터미널 좌표는 시각표로 검증한다**(`pnpm terminals:build` → `terminals:verify`).
  TAGO 가 좌표를 안 줘서 카카오로 지오코딩하는데, 짧은 이름은 엉뚱한 곳을
  짚는다("상봉"이 진주로, "김포"가 고양으로). 사람이 700곳을 검수할 수는
  없으므로 시각표가 대신 따진다 — 닻이 되는 큰 터미널까지의 소요 시간과
  좌표상 직선 거리를 견줘, 버스가 낼 수 없는 속도(25~110km/h 밖)면 버린다.
  **닻 둘이 동의해야** 통과다. 하나만 보면 우연에 속는다 — "연무대"(논산)가
  동수원으로 찍혔는데 부산까지 거리가 비슷해 한 닻으로는 그냥 통과했다
- **좌표표는 더하기만 한다.** 표에 없는 터미널은 예전처럼 이름으로 맞추므로
  표가 비어도 오늘 되는 것은 그대로 된다. 판단이 안 서는 133곳을 굳이 넣지
  않는 이유다 — 조용히 틀린 좌표가 못 찾는 것보다 나쁘다
- **TAGO 시내버스 데이터에 서울이 없다.** 버스노선정보·정류소정보는 지자체가
  넘겨준 것만 담는데 서울시는 자체 API 를 쓴다. 그래서 서울은
  `ws.bus.go.kr` 로 따로 묻는다(`server/busRoutes.ts` 의 `BusRegion`).
  인증키는 같은 공공데이터포털 키다
- **서울 API 는 https 가 안 된다.** TLS 핸드셰이크가 실패해서 http 로 부른다.
  서버끼리의 호출이고 노선 정류장 목록에는 비밀이 없어 감수한 것이다
- 서울 좌표를 TAGO 에 물으면 **인천(23)** 정류소가 오는 일이 있다(관악IC 같은
  광역버스 정류소). 그래서 서울 네모 안이면 서울 API 를 먼저 본다. 잘못
  골라도 정류장 이름과 좌표 200m 를 함께 보는 관문이 엉뚱한 노선을 걸러낸다 —
  인천 자료에 같은 번호가 있어도 안 걸리는 것을 시험했다(2026-09-07)
- **시외 후보에는 접근·도착 구간을 전부 붙인다.** 예전에는 호출을 아끼려고
  상위 두 개에만 붙였는데, 세 번째부터는 "유성복합터미널에서 출발" 로 시작하는
  반쪽짜리 경로가 되어 거기까지 가는 길도 없이 도보 0분·환승 없음이라고
  적혔다(2026-09-07). 후보는 많아야 수단 수(넷)라 나란히 돌리면 값이 얼마 안
  든다. **양 끝 중 하나라도 못 이으면 그 후보는 내놓지 않는다** —
  갈 방법을 못 찾았으면 그건 경로가 아니다
- **연장은 "마지막으로 타는 것" 이 버스일 때만 한다.** 버스 뒤에 지하철이
  오면 그 버스는 목적지가 아니라 환승역으로 가는 길이다. 늘리면 갈아탈 곳을
  지나쳐 버린다 — 강남역 → 건국대에서 논현역 대신 광림교회까지 타라는 답이
  나왔다(둘 다 건국대와 멀다)
- **노선 목록은 갈 때와 올 때가 한 줄로 이어져 있다.** 603 은 100곳 중 31곳이
  이름이 두 번 나온다(목원대학교가 순번 1과 100). **정류장은 반드시 좌표로
  짚을 것** — 이름으로 첫 번째를 잡으면 반대 방향을 짚어 왔던 길을
  되돌아가라는 답이 된다
- **TAGO 는 터미널만 알고 경유 정류소는 모른다.** 대전 → 인천공항 리무진은
  실제로 정부청사(둔산)에 서지만, 그 정류소는 시외 339곳·고속 404곳 목록
  어디에도 없다. 그래서 둔산동 사람도 대전복합터미널까지 가서 타라고 안내된다.
  시간표 없이 "대전복합 출발 +20분" 으로 추정해 채우지 말 것 — 몇 시에 나가야
  하는지가 전부인 앱에서 출발 시각을 지어내면 그게 제일 큰 거짓말이 된다
- **허브 후보는 TAGO 가 아는 이름만 남긴다**(`hubsNear`). 카카오는 작은
  정류소까지 알려주는데, 조회할 수 없는 이름이 세 자리를 차지하면 한 번도
  물어보지 못한 채 후보가 끝난다. 실제로 둔산동에서 시외 허브 세 자리를
  대전청사 정류소 셋(0.8·1.2·1.3km)이 가져가, 공항버스가 뜨는
  대전복합터미널(4.7km)이 밀려나 환승 3회짜리 시내 경로가 답이 됐다
  (2026-09-05). 목록을 못 받아왔을 때는 **전부 통과시킨다** — 거기서 걸러버리면
  TAGO 가 잠깐 죽었을 때 시외 경로가 통째로 사라진다
- **인천공항은 T2 로 들어가 T1 으로 건너간다.** 시외버스 목록에 인천공항1터미널이
  없고 2터미널만 있다(고속에는 둘 다 있지만 대전발 편성이 0이다). T1 행 경로에
  공항 셔틀 구간이 붙는 건 그래서다
- **p-e.kr은 Public Suffix List에 없다.** Let's Encrypt 한도를 남과 나눠 쓴다
  (Caddy가 실패하면 ZeroSSL로 넘어가므로 대개는 발급된다)
- **사용법(`Tour.tsx`)은 선택자로 화면을 짚는다.** `#field-to`, `.panel__submit`
  같은 자리를 가리키므로, 그 선택자를 바꾸면 짚을 곳을 잃는다. 요소를 옮기거나
  클래스를 갈 때는 `STEPS` 를 같이 볼 것
- **없는 데이터는 없다고 말한다.** 어댑터 실패는 예외가 아니라 값(`{ok:false}`)이고
  화면은 `<DataGap>`으로 그 사실을 보여준다. 추정치로 메우지 않는다

## 개발

```bash
pnpm dev:all      # vite + api 동시에
pnpm build
pnpm exec vitest run
pnpm exec tsc -b --force && pnpm exec tsc --noEmit -p tsconfig.server.json
```
