<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- 웹뷰 위아래로 남는 시스템 영역을 판 색으로 칠한다 (colors.xml 주석 참고) -->
        <item name="android:windowBackground">@color/whenigoInk</item>
        <item name="android:statusBarColor">@color/whenigoInk</item>
        <item name="android:navigationBarColor">@color/whenigoInk</item>
        <!-- 어두운 바탕이므로 상태바 아이콘은 밝게 -->
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>